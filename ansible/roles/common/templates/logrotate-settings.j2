/var/log/raspberry-noaa-v2/*.log {
  missingok
  daily
  rotate 7
  nocompress
  notifempty
  # world-readable: the webpanel (running as the webserver user) tails this
  # log for the live capture status
  create 0644 {{ target_user }} {{ target_user }}
}
